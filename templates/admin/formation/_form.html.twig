{{ form_start(formFormation) }}
{{ form_row(formFormation.titre) }}
<div class="d-grid gap-2 mt-3">
	<button class="btn btn-primary" type="submit">
		{{ submitButtonText|default('Créer') }}
	</button>
</div>
{{ form_end(formFormation) }}
