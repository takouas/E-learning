<nav class="navbar navbar-expand navbar-dark bg-dark" aria-label="Navbar">
	<div class="container">

		<a class="navbar-brand" href="{{ path("app_home") }}">
			Tek-Up
		</a>
	{% if is_granted('ROLE_ENSEIGNANT') %}
					<a class="navbar-brand" href="{{ path("app_classedecours_creer") }}">Nouvelle classe</a>
					<a class="navbar-brand" href="{{ path("app_home") }}">Mes classes</a>
				
    {% endif %}
		{# Navigationel link #}
		<ul class="navbar-nav ms-auto me-2">
			{% if is_granted('ROLE_ENSEIGNANT') %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path("app_classedecours_creer") }}" data-bs-toggle="tooltip" data-bs-placement="bottom" title="Nouvelle classe">
						<img src="{{ asset('icons/bookmark-plus.svg')}}">
					</a>
				</li>
			{% endif %}
		</ul>
		{# end Navigationel link #}


		{# Dropdown Profil #}
		<div class="dropdown text-end">
			<a href="#" class="d-block link-dark text-decoration-none dropdown-toggle ps-3" id="dropdownUser1" data-bs-toggle="dropdown" aria-expanded="false">
				<img class="rounded-circle" height="32" width="32" src="{{ app.user.photoDeProfil is null ? asset("img/photo-avatar-profil.png") : vich_uploader_asset(app.user) }}">
			</a>
			<ul class="dropdown-menu dropdown-menu-end text-small text-center" aria-labelledby="dropdownUser1">

				<div class="d-flex flex-column text-center px-2">
					<span class="fw-bold">
						{{ app.user.fullName }}
					</span>
					<span class="fw-normal">
						{{ app.user.email }}
					</span>
					<span>
						<a href="{{ path("app_profil_index") }}" class="text-decoration-none">Mon profil</a>
					</span>

				</div>
				{% if is_granted('ROLE_ENSEIGNANT') %}
					
					<li><hr class="dropdown-divider"></li>
					<li>
						<a class="dropdown-item" href="{{ path("app_classedecours_creer") }}">Nouvelle classe</a>
					</li>
					<li>
						<a class="dropdown-item" href="{{ path("app_home") }}">Mes classes</a>
					</li>
				{% endif %}
				<li><hr class="dropdown-divider"></li>
				<li>
					<a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a>
				</li>
			</ul>
		</div>
		{# end dropdown Profil #}

	</div>
</nav>
